<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white"
    android:padding="8dp">

    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/add_new_gift_card"
                style="@style/Animation.Design.BottomSheetDialog"
                android:layout_width="362dp"
                android:layout_height="match_parent"
                android:text="Nueva Tarjeta de Regalo"
                app:chipBackgroundColor="@color/amazon_blue"
                app:chipStrokeColor="@color/primary_color"
                app:icon="@drawable/ic_add">

            </com.google.android.material.button.MaterialButton>
        </LinearLayout>
    </HorizontalScrollView>

    <!-- Contador de resultados -->
    <TextView
        android:id="@+id/results_count"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Mostrando tarjetas de regalo"
        android:textColor="@color/gray"
        android:layout_marginBottom="8dp"/>

    <!-- Lista de tarjetas -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_gift_cards"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingBottom="16dp"
        tools:listitem="@layout/item_gift_card"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"/>

    <!-- Botón flotante para agregar -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add_gift_card"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|bottom"
        android:layout_margin="16dp"
        android:src="@drawable/ic_add"
        app:backgroundTint="@color/primary_color"
        app:tint="@color/white"/>
</LinearLayout>