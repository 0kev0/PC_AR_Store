<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="8dp">

    <androidx.cardview.widget.CardView
        android:layout_width="300dp"
        android:layout_height="180dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="8dp"
        app:cardBackgroundColor="@color/card_background">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_gift_card_gradient"
            android:padding="16dp">

            <!-- Logo en esquina superior izquierda -->
            <ImageView
                android:id="@+id/ivCardType"
                android:layout_width="80dp"
                android:layout_height="50dp"
                android:layout_alignParentTop="true"
                android:layout_alignParentStart="true"
                android:src="@drawable/azamoz_logo2" />

            <!-- Nombre en esquina superior derecha -->
            <TextView
                android:id="@+id/tvGiftCardName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentTop="true"
                android:layout_alignParentEnd="true"
                android:text="GIFT CARD"
                android:textColor="@android:color/white"
                android:textSize="14sp"
                android:textStyle="bold" />

            <!-- Valor en el CENTRO -->
            <TextView
                android:id="@+id/tvGiftCardPrice"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:text="$100"
                android:textColor="@android:color/white"
                android:textSize="35sp"
                android:textStyle="bold" />

            <!-- Número de tarjeta en esquina inferior izquierda -->
            <TextView
                android:id="@+id/tvCardNumber"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentStart="true"
                android:layout_alignParentBottom="true"
                android:text="PDM-••••••"
                android:textColor="@android:color/white"
                android:textSize="14sp" />

            <!-- Botón en esquina inferior derecha -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnBuy"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="120dp"
                android:layout_height="40dp"
                android:layout_alignParentEnd="true"
                android:layout_alignParentBottom="true"
                android:layout_marginEnd="13dp"
                android:layout_marginBottom="-1dp"
                android:text="COMPRAR"
                android:textAllCaps="false"
                android:textColor="@color/text_lighter_color"
                android:textSize="14sp"
                app:backgroundTint="@color/white"
                app:cornerRadius="8dp"
                app:icon="@drawable/ic_carrito"
                app:iconGravity="textStart"
                app:iconPadding="4dp"
                app:iconTint="@color/text_light_color"
                app:rippleColor="@color/text_light_color"
                app:strokeColor="@color/text_light_color"
                app:strokeWidth="1dp" />

        </RelativeLayout>
    </androidx.cardview.widget.CardView>
</LinearLayout>